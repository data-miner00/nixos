# Alias
alias switch='sudo nixos-rebuild switch -I nixos-config=$HOME/configuration.nix'
alias config='/run/current-system/sw/bin/git --git-dir=$HOME/.dots --work-tree=$HOME'

alias v="NVIM_APPNAME=vnext nvim"

neofetch
